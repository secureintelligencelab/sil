/* =====================================================================
   SUPABASE CONFIGURATION
   ---------------------------------------------------------------------
   These are PUBLIC keys — safe to commit to GitHub. The publishable
   (anon) key only allows the actions your Row-Level-Security policies
   permit, so security lives in the database, not in this file.

   If you ever rotate your key or change project, update the two values
   below only.
   ===================================================================== */

const SUPABASE_URL = "https://vblvukqtcdtltmyuglzc.supabase.co";
const SUPABASE_KEY = "sb_publishable_sMcRaHfjNI9XOoSLDEXF9Q_ONd8ZQ24";

/* The admin can edit / delete / approve ANY event.
   Every other user registers their own account and can only manage their
   OWN submissions. */
const ADMIN_EMAIL = "ashraf.uddin@cihe.edu.au";
